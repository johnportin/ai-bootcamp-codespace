# ai-bootcamp-codespace
Codespace for RAG to Agents bootcamp

to start your notebook, run

```
uv run jupyter notebook
```

or if that fails, try

```
uv run jupyter lab --no-browser --ip=0.0.0.0 --port=8888

```